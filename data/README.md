# Data scope

No patient rows, original source bundles, held-out answer keys or provider raw
responses are included here. Aggregate and selected-summary records are under
`artifacts/`. See [the data card](../docs/data_card.md).

This directory preserves the familiar navigation point without pretending that
omitted datasets are present.
